#include <stdio.h>
int main() {
    int A, B, X; 
    // Leitura dos dois valores inteiros
    scanf("%d", &A);
    scanf("%d", &B);    
    // Soma de A e B
    X = A + B;    
    // Impressão do resultado conforme o formato solicitado
    printf("X = %d\n", X);    
    return 0;
}
