#include <stdio.h>
int main() {
    int A, B, PROD;
    // Leitura dos dois valores inteiros
    scanf("%d", &A);
    scanf("%d", &B);
    // Cálculo do produto
    PROD = A * B;
    // Impressão do resultado no formato correto
    printf("PROD = %d\n", PROD);
    return 0;
}
